$(document).ready(function(){
    $('.promo__slick_slider').slick({
      arrows: true,
      dots: true,
      draggable: false,
      
    });
  });





  // function solution(number) {
  //   let arr = new Array(number).fill(number).map((_, x) => x + 1);
  //   arr.length = arr.length - 1
  //  return arr.length < 3 ? 0 : arr.filter(x => (x % 3 === 0) || (x % 5 === 0)).reduce((x, y) => x + y)
    
    

    
  // }

  // console.log(solution(10));